export type queryType = {
    data: {
        region_type_full: string
        region: string
        street_type_full: string
        street: string
        settlement_type: string
        settlement: string
    }
}